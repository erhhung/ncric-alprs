export HOST="DATASTORE"
export DEFAULT_USER="ubuntu"

export ENV="${ENV}"
export S3_URL="${S3_URL}"
export GITHUB_TOKEN="${GITHUB_TOKEN}"
export FROM_EMAIL="${FROM_EMAIL}"
export BACKUP_BUCKET="${BACKUP_BUCKET}"
export CONFIG_BUCKET="${CONFIG_BUCKET}"
export CONDUCTOR_IP="${CONDUCTOR_IP}"
