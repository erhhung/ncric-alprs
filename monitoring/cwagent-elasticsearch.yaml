# this file will be merged with "cwagent-main.yaml"

metrics:
  metrics_collected:
    disk:
      resources:
        - /
        - /opt/elasticsearch

logs:
  logs_collected:
    files:
      collect_list:
        - file_path: /opt/elasticsearch/logs/*.log
          log_group_name: astrometrics
          log_stream_name: elasticsearch
          retention_in_days: 90
